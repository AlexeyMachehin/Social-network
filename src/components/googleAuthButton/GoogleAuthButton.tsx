import { useState } from 'react';
import classes from './googleAuthButton.module.css';

export default function GoogleAuthButton() {
 

  return (
    <button className="container mainButton">Enter with Google account</button>
  );
}
